@model IEnumerable<Projet2020.Models.Panier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_prod)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_commande)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_prod)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_commande)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pan }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pan })
        </td>
    </tr>
}

</table>
